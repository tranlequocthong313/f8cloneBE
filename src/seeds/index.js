const seedVideo = require('./video')
const seedCourse = require('./course')
const seedJob = require('./job')
const seedUser = require('./user')

module.exports = {
    seedVideo,
    seedCourse,
    seedJob,
    seedUser
}
